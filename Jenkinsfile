pipeline {
   agent none 
   stages {
        stage('Example') {
            steps {
            // One or more steps need to be included within the steps block.
            git 'https://github.com/SpareBank1/monorepo-sample.git'
            }
        }
    }
}